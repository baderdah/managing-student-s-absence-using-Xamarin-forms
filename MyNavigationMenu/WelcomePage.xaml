<?xml version="1.0" encoding="UTF-8"?>
<ContentPage
    BackgroundColor="DarkCyan"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="MyNavigationMenu.WelcomePage">
        <Grid Padding="20" >
        <Grid.RowDefinitions>

            <RowDefinition Height="*" />
            <RowDefinition Height="50" />
            <RowDefinition Height="50" />
           
        </Grid.RowDefinitions>

        <StackLayout
            Grid.Row="0"
            VerticalOptions="Center"
            >
            <Label
                Text="Welecom to CheckIt"
                TextColor="White"
                FontSize="30"
                FontAttributes="Bold"
                HorizontalOptions="Center"
                />
            <Label
                Text="Manage your classes more easily"
                TextColor="White"
                FontSize="18"
                FontAttributes="Italic"
                HorizontalOptions="Center"
                />
        </StackLayout>

        <Button
            Clicked="loginBtn_Clicked"
            x:Name="loginBtn"
            BackgroundColor="CadetBlue"
            TextColor="White"
            Grid.Row="1"
            Text="Login"/>
        <Button
            Clicked="loginBtn_Clicked"
            x:Name="registerBtn"
            BackgroundColor="CadetBlue"
            TextColor="White"
            Grid.Row="2"
            Text="Register" />
    </Grid>
</ContentPage>
